version: 2
updates:
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"

  # Go module dependencies (vendored; vendorHash=null in flake.nix).
  - package-ecosystem: "gomod"
    directory: "/"
    schedule:
      interval: "weekly"
