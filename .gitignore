# Override global ignore: check pyrightconfig.json into the repo so
# everyone gets the same type-check setup.
!pyrightconfig.json

result
.run-result
.last-load
.last-load-*
.direnv/
.venv/
__pycache__/
*.pyc
.yolo/
*.egg-info/
probe.log
dist/
dist-go/
build/
# Stray root-level Go build artifacts (a bare `go build ./cmd/<x>` drops the
# binary here). The real build channel is dist-go/; these should never be
# tracked (a stale root yolo-parity was committed once — see the audit).
/goprobe
/yolo
/yolo-parity
/yolo-broker-relay
/yolo-host-processes
/yolo-ps
/yolo-cgd
/yolo-journald
/yolo-jail-supervisor
/yolo-oauth-terminator
/yolo-claude-oauth-broker-host
/yolo-entrypoint
src/_version.py
.DS_Store
.research/
yolo-jail.local.jsonc

# audit/test scratch artifacts (reviewer-created; never commit)
/scratchpad/
