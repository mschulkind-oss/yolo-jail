---
title: "Roadmap"
date: 2026-09-13
status: accepted
tags: [roadmap, routing, open-questions]
summary: "The routing table for open work: one line per decision, the doc that holds it, and what ruling it releases — ordered so the next hour is obvious."
vantage:
  status-chip: true
---

# Roadmap

**Status:** CURRENT — 2026-09-19. **57 rows**: 6 to rule first, 28 needing a decision,
14 ready to build, 7 waiting, 2 iced. Three rows closed on 2026-09-18 and left the file:
[`OQ-2`](../design/cerebras-pack-and-copilot-delivery.md#decision-ledger), whose fix shipped as `25d46b7d`, and the two protocol-resolution questions its design absorbed. Three 📦 rows CLOSED on 2026-09-18 and left the file
(`eb02ad86`, `67cf4c81`, `f5c26899`): the supervisor's swallowed spawn failure, `provides`
leaking into four agents' config files, and the capability gate `yolo check` never ran.

This file is a **routing table, not a place to think**: one line per open decision, naming
the doc that holds it and what a ruling releases. It is **not a record of what happened** —
work that closes leaves the file, and `git log -p` is the history. Every count here is
derived rather than carried forward; re-derive the live-question totals with check 2 of
[`README.md`](README.md#keeping-this-corpus-honest--the-five-checks-so-they-are-re-runnable):

```console
$ rg -c '^(#{2,4} |\s*[0-9]+[a-z]?\. |\s*[-*] )(<a id="[^"]*"></a> ?)?💬' docs/ --sort path
```

The command above is the count, and this line deliberately does not restate it: the number moves every time a question is ruled, and a frozen one here was **off by one within a day** of being written. Run it. ⚠ It misses one 🔒, which no regex over `docs/` can see.

## Rule these first

**The ordering basis, so it is checkable:** a defect live in shipped code outranks blocked
build work, which outranks a ruling that only closes a doc; ties break toward the smallest
sitting. Every row below is the first class.

| | Rule | Releases | Cost |
|---|---|---|---|
| **0** | Stop `yolo host apply` adopting claude.ai's skills-sync root | **Data loss, live today, on any host with claude.ai Skills sync enabled — MEASURED, not reasoned.** `hostskills.Adoptions` offers `~/.claude/skills/synced/` as ONE adoption named `synced`, because it clears every exclusion: it is not dot-prefixed (`.trash` and `.staging` are, and are correctly skipped), it IS a directory, it is in no ownership record, and `pluginpack.ManifestPath` finds no manifest at that level — the manifests are **two levels down**, one per identity bucket. An `apply --assert` then MOVES the whole sync root into `~/.config/yolo-jail/local/skills/synced/` and composes a byte-identical copy back, so the tree looks untouched and nobody notices. **The loss lands on the NEXT apply after an upstream sync**: a new skill is deleted and an edited one reverted, reported as `Applied: 1 composed skill.` ⚠ The fix is a FENCE before any copier — [`synced-skill-trees.md`](../design/synced-skill-trees.md)'s `P1` — and it is separable from every ruling that doc leaves open. Whether the fence is pack-declared or core-known is [`OQ-ST2`](../design/synced-skill-trees.md#OQ-ST2); the fence itself is not a ruling | one fix, then the four `OQ-ST` questions at leisure |
| **0b** | Give `dropComputedTables` a leaf-level signal, so an adopting render stops dropping the user's other enabled plugins and env vars | **Data loss, live today, DOCUMENTED, and it has already FIRED on the maintainer's host.** `internal/agentcfg/staterender.go`'s `dropComputedTables` removes every top-level key the COMPUTED layer holds as an OBJECT, betting that such a key is yolo's own output from a previous boot. **The bet is key-level and the computed layer is not**, so it over-drops whenever a derive returns an object it only partly fills. `packs/claude/derive.lua` returns `enabledPlugins` asserting only its three LSP plugin ids and `env` asserting only `ENABLE_LSP_TOOL`, so an adopting boot drops **the user's OTHER enabled plugins and the agent's OTHER env vars wholesale**. ⚠ **Measured on the maintainer's host 2026-09-19**: `~/.claude/settings.json`'s `enabledPlugins` now holds exactly yolo's two computed LSP toggles after a host render, which is what this path does to anything a `claude plugin install` had enabled. That is very likely the registration a deleted plugin directory was regenerating FROM. ⚠ It is PRE-EXISTING rather than a regression — the blanket pure-render drop it replaced took both keys too — and the boundary is stated at the function rather than papered over, with `internal/entrypoint/hostownedadoption_test.go` scoping it out explicitly. **Closing it needs a leaf-level signal the function does not have**: which leaves under a computed table the derive actually asserted. The `permissions` fix one key over on the same file is the shape that already worked | a leaf-level assertion record from the derive, then the drop narrows to it |
| **1** | Emit the host-service endpoint for EVERY active `scope: "host"` loophole, not for two names | **Defect, live today, and it makes a shipped feature inert.** `internal/cli/run/assemble_parts.go:480-491` (`hostServicesMountArgs`) emits `YOLO_SERVICE_<NAME>_ENDPOINT` for `claude-oauth-broker` and `openai-auth` **by name**. `aws-auth` is the third host-scoped loophole ever and gets nothing, so its adapter answers `ServiceUnreachable` for every request — while the launch reports a healthy jail, because the reachability witness walks `YOLO_SERVICE_*_ENDPOINT` and the fault IS that no such variable exists. A third hardcoded name is the wrong fix and that function's own comment argues for the right one: the ACTIVE-and-may-run-host-code predicate those two already compute, applied to every host-scoped loophole. ⚠ The same function returns `nil` outright on Apple Container unless `openai-auth` is active, so that backend skips the sockets mount too. ⚠ **One of the two exceptions this ruling is about has lost its reason.** `brokerEndpointIsUnpublishable`'s nested-launch arm was justified by “yolo's image bakes no openssl, and the broker daemon needs it to mint its CA” — both halves are now gone (`431625bc` baked it, `d5bb1e5d` removed the need), and a nested launch has been MEASURED minting its own P-256 CA and publishing its endpoint. Its second half never depended on openssl and still stands (the loophole's CA state files are not in a nested launcher's storage), and [`OQ-2`](../design/broker-ca-and-nested-hosts.md#7-open-questions) ruled that nested jails run their own broker — which is an argument for deleting the arm outright. Left standing and stated in place (`0de243cb`) rather than changed in passing, because it is the same decision as this row's | one ruling — whether `brokerEndpointIsUnpublishable`'s two exceptions are per-loophole or per-launch — then the generalisation |
| **2** | [`OQ-BR4`](../design/bedrock-plumbing.md#OQ-BR4) — narrow `profile`'s env gate | **Defect, live today.** `internal/packload/packload.go:634` matches a bin *any* selected pack installs, so one agent's profile fires another's env | one ruling |
| **3** | [`OQ-PS2`](../design/provider-switching.md#OQ-PS2) — clear the model id a deselect orphans | **Defect, live today.** `internal/agentcfg/selection.go:168` only ever lifts, so a dropped profile leaves its model pinned | one ruling |
| **4** | Stop the incompatible-daemon warning naming `yolo broker restart` for the two host singletons it cannot restart | **Defect, live today, and it is in the sentence that presents itself as the fix.** `yolo broker status\|stop\|restart\|logs` is Claude-only — [`help.go`](../../internal/cli/help.go) says *"Manage the Claude OAuth broker"* and the command bodies wire `CLIRealDeps()` to the broker's own `RealDeps()`, its name and argv hardcoded. `startHostSingleton` ([`loopholesruntime.go`](../../internal/cli/run/loopholesruntime.go)) is generic: it interpolates whichever singleton is ALIVE-BUT-INCOMPATIBLE into a warning that says token refresh is *"broken on this host, silently"*, then ends `Fix it with: yolo broker restart` — which for `openai-auth-broker` or `aws-auth` cycles a **different** daemon and leaves the broken one running. `aws-auth` has no management verb of any kind: no status, no logs, no stop, nothing short of `kill(1)`. ⚠ **The one-loophole scope EXPIRED rather than being wrong**, which is why this is a ruling and not an oversight: the reason recorded at the time is [`brokerlifecycle.go`](../../internal/broker/brokerlifecycle.go)'s package comment — *"The broker is the only loophole that declares it today"* — and it stopped being true on 2026-09-15 when `openai-auth-broker` landed; `aws-auth` made it three on 2026-09-18. The lifecycle engine already generalized (`SingletonDeps` derives every path from a loophole name); the CLI is the half left behind, and the sibling `reportFailedSpawn` names the loophole from the record for exactly this reason | one ruling — [`OQ-HD2`](../design/host-daemon-ownership.md#OQ-HD2) — then a verb over the host-scoped set, `broker` kept as an alias |

## 💬 Needs you

| # | Decides | Doc | Live | Gate | Releases |
|---|---|---|---|---|---|
| 1 | Whether the backend census is worth building, and whether a `Warned` disposition can be suppressed | [`backend-parity.md`](../design/backend-parity.md) · in-review | 2 | [`OQ-BP-1`](../design/backend-parity.md#open-questions) | **build** — the census itself. `OQ-BP-5` left this row on 2026-09-15, answered by code (an ACE) rather than by a ruling |
| 4 | A stated byte budget, or a policy — **now measured** | [`minimal-disk-footprint.md`](../design/minimal-disk-footprint.md) · in-review | 1 | [`OQ-DF4`](../design/minimal-disk-footprint.md#OQ-DF4) | **doc** — unblocked 2026-09-15; ~70 GiB/yr grows unreclaimed but 99% is two NAMED stores, so the call is "sweep `mise/` + `cache/staticcheck`, or declare them yours" |
| 5 | Provider deselection, aliases and shipped model ids | [`provider-switching.md`](../design/provider-switching.md) · draft | 4 | [`OQ-PS2`](../design/provider-switching.md#OQ-PS2) | **defect** — an orphaned model pin |
| 8 | Bedrock's schema marker, names, region and env gate | [`bedrock-plumbing.md`](../design/bedrock-plumbing.md) · draft | 7 | [`OQ-BR4`](../design/bedrock-plumbing.md#OQ-BR4) | **defect** — a cross-agent profile leak |
| 9 | Whether a mistyped reference refuses or only reports | [`reference-mismatch-diagnostics.md`](../design/reference-mismatch-diagnostics.md) · in-review | 4 | [`OQ-RM2`](../design/reference-mismatch-diagnostics.md#OQ-RM2) | **defect** — the diagnostic reaches bare stderr only |
| 10 | What the environment manager promises at each notch | [`environment-manager-user-stories.md`](../design/environment-manager-user-stories.md) · no frontmatter | 11 | Q8, [§Open Questions](../design/environment-manager-user-stories.md#open-questions) | **defect** — `internal/cli/briefing.txt:5` says JAIL at every notch |
| 11 | Skill and overlay collisions, and `host_files` modes | [`BACKLOG.md`](BACKLOG.md) Stage E · current | 7 | [`E2`](BACKLOG.md#-e2--readonly-as-a-real-ro-mount-instead-of-0o444) | **defect** — two silent last-one-wins paths |
| 12 | Who provisions a binary at each notch, and in what order | [`provisioner-sets.md`](../design/provisioner-sets.md) · in-review | 13 | [`OQ-PS1`](../design/provisioner-sets.md#OQ-PS1) | **build** — the host notch's provisioner |
| 13 | The contribution shape that replaces `mcp_presets` | [`mcp-presets-removal.md`](../design/mcp-presets-removal.md) · in-review | 6 | [`OQ-MP3`](../design/mcp-presets-removal.md#OQ-MP3) | **build** — a pack-shipped MCP kind |
| 14 | Whether `program` below `jail` is refused or confirm-gated | [`yolo-as-environment-manager.md`](../design/yolo-as-environment-manager.md) · no frontmatter | 2 | [`OQ-EM1`](../design/yolo-as-environment-manager.md#OQ-EM1) | **build** — env-manager Phase 4.3 |
| 15 | How a dotted leaf in `packages:` resolves | [`package-nested-attribute-paths.md`](../design/package-nested-attribute-paths.md) · draft | 1 | [`OQ-1`](../design/package-nested-attribute-paths.md#OQ-1) | **build** — the resolver, or nothing |
| 16 | Whether program-delivery steps three and five survive | [`program-delivery.md`](../design/program-delivery.md) · in-review | 1 | [`OQ-PD19`](../design/program-delivery.md#-oq-pd19--do-steps-three-and-five-still-have-a-subject-after-the-agentproject-split) | **build** — or a retirement |
| 17 | How a pack ships a binary of its own | [`broker-as-a-pack.md`](../design/broker-as-a-pack.md) · no frontmatter | 2 | [`OQ-BP5`](../design/broker-as-a-pack.md#OQ-BP5) | **build** — and the question narrowed on 2026-09-17, when the mechanism was actually run (`packs/hello-daemon`). Declaration is fine and needs no schema change; DELIVERY is half-broken, and not fixably: an EMBEDDED pack cannot carry an exec bit at all, because `embed.FS` reads every file back as `0444`. So this now decides how an OFFICIAL pack ships a program, given that the route [§3.1](../design/broker-as-a-pack.md#31-what-is-actually-unresolved-here) rated ✅ works only for a pack configured by path |
| 18 | What replaces `macos-26-intel` before 26.05 lapses | [`macos-support-matrix.md`](../research/macos-support-matrix.md) · live tracker | 1 | [§5 item 8](../research/macos-support-matrix.md#5-roadmap-ordered) | **build** — a CI commitment, with a 2026 deadline |
| 19 | What an attach does with the attacher's pack set | [`agent-config-packs.md`](agent-config-packs.md) · overtaken | 4 | [`OQ-ACP3`](agent-config-packs.md#-oq-acp3--whether-the-prism-should-become-a-standalone-tool-that-also-manages-host-configs) | **defect** — `internal/cli/run/run.go:766` re-renders before the attach branch at `:774` |
| 20 | Whether `yolo host apply` is a convenience or the path | [`host-render-target.md`](../design/host-render-target.md) · no frontmatter | 3 | 9.2, [§9](../design/host-render-target.md#9-open-questions--the-discussion-part) | **doc** — product posture; [§8](../design/host-render-target.md#8-what-i-would-actually-do-in-order) step 3 is in 📦 regardless |
| 21 | Whether the jail mounts the workspace at the host's path | [`workspace-path-mirroring.md`](../design/workspace-path-mirroring.md) · draft | 12 | [`OQ-WP8`](../design/workspace-path-mirroring.md#OQ-WP8) | **doc** — ratify the no |
| 22 | Whether `macos-user`'s workspace root becomes configurable, and what the profile has to derive from it | [`configurable-workspace-root.md`](../design/configurable-workspace-root.md) · in-review | 4 | [`OQ-CW1`](../design/configurable-workspace-root.md#OQ-CW1) | **build** — the read-deny derived from the root, plus the whitelist that closes four lexical bypasses. ⚠ The whitelist wants to land before anyone relies on the current blacklist; withdrawing an accepted path later is a breaking change |
| 23 | How a `jail_daemon` runs on `macos-user` — argv resolution with no image, and whether it is confined | [`jail-daemon-on-macos-user-plan.md`](../design/jail-daemon-on-macos-user-plan.md) · draft | — (no ids, so the count below cannot see them; both are stated in that plan's §Blockers) | [§Blockers](../design/jail-daemon-on-macos-user-plan.md) | **build** — and it gates the blocked halves of BOTH 📦 [row 1](#-ready) and [row 3](#-ready), which is why it is one row. Every shipped `jail_daemon.cmd` names `yolo-jaild`, which exists only as `bin/linux-<arch>`, and a symlink cannot reach it because both binaries dispatch on plain `args[0]` rather than `argv[0]`. And `DP-L3`'s prescribed placement in `buildBootstrapEnv` would start the daemon UNCONFINED and orphaned, which the doc is silent about |
| 24 | Whether a repo's own skills may reach whichever agent the reader chose | [`workspace-skills.md`](../design/workspace-skills.md) · in-review | 6 | [`OQ-WS1`](../design/workspace-skills.md#OQ-WS1) | **build** — or a close. A repo that ships `.claude/skills/` has chosen for its readers, and a copilot or codex user gets a worse experience from the same checkout for no reason either of them chose. Four mechanisms are costed against measurements (where each agent reads at project scope, which dedupe by bare name vs real path, and the writability asymmetry that makes the workspace the only jail-side landing place). [`OQ-WS1`](../design/workspace-skills.md#OQ-WS1) is the closure question and it is a security question: `packs` is user-scope ONLY because the workspace is jail-writable, so whether a workspace may contribute CONTENT at all is the thing to rule — a no makes the deliverable the documented convention in [§4.3](../design/workspace-skills.md#43-baseline-c--conventions-only) and closes the doc |
| 26 | Whether `mounts` and `env_sources` become user-scope-only, like every other key that grants host access | [`agent-safehouse.md`](../research/agent-safehouse.md) · in-review | 1 | [`OQ-AS3`](../research/agent-safehouse.md#OQ-AS3) | **defect-shaped, and it is an ASYMMETRY rather than an oversight.** `packs`, `profiles`/`use_profiles`, source-bearing `host_files`, loophole `settings` and a provider's `base_url` are each refused in a workspace config BY RULING — because that file is agent-editable and travels with the repo. `mounts` (a host dir at `/ctx`) and `env_sources` (a host dotenv whose values become the jail's environment) are NOT: verified 2026-09-18, neither has a workspace-scope refusal anywhere in `internal/config`. So a repo-committed `yolo-jail.jsonc` can name a host path to mount and a host file to read secrets out of, disclosed only by the config-change diff. The comparison that surfaced it argues the fix is a scope rule rather than a trust prompt, which is what [`gate-placement-principle.md`](../reference/gate-placement-principle.md) would say too — `env_sources` is the sharper half, since source-bearing `host_files` is already user-scope-only for exactly this reason and `env_sources` reaches the same host files by another name |
| 25 | Storage tier and pre-launch update execution for Pi extensions across jails | [`pi-extension-lifecycle.md`](../design/pi-extension-lifecycle.md) · in-review | 3 | [`OQ-1`](../design/pi-extension-lifecycle.md#OQ-1) | **build** — machine-scoped package storage and launcher refresh |
| 27 | Whether the hard-coded Kilo special-casing in two agent derives stays or goes | [`gateway-provider-packs.md`](../design/gateway-provider-packs.md) · accepted | 1 (in the header, not yet an `OQ-`) | [the warning](../design/gateway-provider-packs.md) | **graduation** — and it is the ONLY thing holding that doc back. `caaaae1b` gave `packs/claude/derive.lua` and `packs/pi/derive.lua` a Kilo detector that matches on provider name **or** on an `api%.kilo%.ai` substring of the base URL, a `deepseek-` → `deepseek/` model-id rewrite, and a hard-coded context window. pi's copy also treats a profile's `model` option as a LITERAL model id when the user declared no aliases — which is the direct contradiction of [§3](../design/gateway-provider-packs.md#3-failure-and-safety-rules)'s *“an alias missing from the map writes no selection, rather than substituting a sole model or a gateway default”*. ⚠ The [§4](../design/gateway-provider-packs.md#4-what-this-does-not-propose) *“never hard-coded”* rule survives only on the technicality that this is in a PACK rather than in core — but it is an AGENT pack naming a PROVIDER pack, which is the coupling `packs/` exists to prevent. Stays → amend [§3](../design/gateway-provider-packs.md#3-failure-and-safety-rules) and [`OQ-GP2`](../design/gateway-provider-packs.md#decision-ledger), and add the provenance comment [`providers.md`](../reference/providers.md#derives-the-delivery-mechanism) requires. Goes → three blocks deleted from two derives. Either way the doc can graduate; until then it describes a system that is not there |
| 28 | How packs declare bypassed or unmanaged file diagnostics for yolo check | [`pack-declared-file-diagnostics.md`](../design/pack-declared-file-diagnostics.md) · in-review | 3 | [`OQ-1`](../design/pack-declared-file-diagnostics.md#oq-1) | **build** — declarative traps in `pack.json` or self-check |
| 29 | Who owns a host-wide daemon after the launch that ensured it, and whether its rendezvous carries a version | [`host-daemon-ownership.md`](../design/host-daemon-ownership.md) · in-review | 8 | [`OQ-HD2`](../design/host-daemon-ownership.md#OQ-HD2) | **defect** — a warning naming the wrong command for two of the three singletons ([Rule these first](#rule-these-first) row 4). ⚠ **Nothing is built and nothing is proposed.** The doc makes the settled architecture legible and asks eight rulings, and the honest answer to several may be *"this is right; write it down"* — which is why one row rather than eight. The organising claim is [`OQ-HD1`](../design/host-daemon-ownership.md#OQ-HD1): a daemon rendezvous is a **name** with no version in it, so an old process and a new yolo meet at one path each assuming the other matches, and that single absence produces the alive-but-incompatible standoff, the legacy PID file and the `/proc` reclaimer that landed the same day. It is the one the others hang off, and it is itself blocked on [`OQ-HD6`](../design/host-daemon-ownership.md#OQ-HD6) — a version in the path multiplies daemons across an upgrade and nothing reaps the old one, because no refcount, TTL, idle timer or reaper exists |
| 30 | Whether a required jail daemon that cannot publish should refuse the launch with nothing to get past it | [`loopback-tls-reachability.md`](../reference/loopback-tls-reachability.md) · current | — (no id; neither gate's ruling covers the pair) | [`OQ-R2`](../reference/loopback-tls-reachability.md#oq-r2) | **defect-shaped, and MEASURED on a host 2026-09-19.** Two independent escalations abort one boot and **only one has a hatch**. `YOLO_ALLOW_UNREACHABLE_SERVICES=1` is read inside `reportUnusableServices` ([`reachability.go`](../../internal/entrypoint/reachability.go)) and downgrades the in-jail reachability witness to a warning exactly as documented — and the launch still died as `refusing to start the jail: 1 config generator(s) failed: - start_jail_daemon_supervisor: jail daemon "wire-bridge" cannot publish its required endpoint`, because `startJailDaemonSupervisor` ([`runtime.go`](../../internal/entrypoint/runtime.go)) turns a `failed` readiness line into an error through `genStep`, whose A12 fail-closed policy reads no variable at all. Both land in the same `genFailuresError`; one consults the hatch before escalating and the other cannot. ⚠ **A DESIGNED idle takes the second path**: `serve` signals `failed` *before* idling, so the healthy-idle state 📦 [row 13](#-ready) just made survivable still refuses every launch that reaches it. ⚠ The consequence is the reason for the row: **there is no way to get a shell in a jail whose required jail daemon cannot start** — the one moment a shell is what you need. Whether the refusal is right is genuinely open; what nobody ruled is that the two gates should disagree about being escapable |
| 31 | What `--verbose` means past the host↔jail boundary: a level or a boolean, and whether a boot snapshot at a give-up is always-on | [`diagnostics-past-the-boundary.md`](../design/diagnostics-past-the-boundary.md) · in-review | 4 | [`OQ-DB1`](../design/diagnostics-past-the-boundary.md#oq-db1) · [`OQ-DB2`](../design/diagnostics-past-the-boundary.md#oq-db2) | **defect, live today, and it is in the gate itself.** `internal/entrypoint/runtime.go`'s two daemon-lifecycle notices are gated on `paths.VerboseEnv != "" \|\| YOLO_JAIL_TIMING != ""`, and **the first disjunct can never be true in a jail**: the container env is a closed list of explicit `-e` pairs with no `YOLO_VERBOSE` among them, no path passes `--env-host`, and `macos-user` crosses through `/usr/bin/env -i`. P5/D5 in the [perf-logging reference](../reference/perf-logging.md) *forbids* forwarding it, so this is dead by ruling rather than by accident — and no test in `internal/entrypoint` touches `VerboseEnv`, so the code reads as though the host flag reaches the jail. ⚠ **`--verbose` is not a placeholder** (only `verbose.go`'s header still says so; [`OQ-RO2`](../reference/report-tiers.md#why-its-this-way) spent the reservation on 2026-09-12); what it has is three host-side meanings and **zero** jail-side ones. Consequence, measured on the 8214 collision: `portInUse` answers *occupied* by binding and can never answer *by whom*, there is no `/proc/net/tcp` parsing in the tree, and the one silent branch in a five-branch function is the port-collision branch. Separately measured: **153 sites** where the give-up is documented and still reports nothing, against **316** where it was made loud, and **8 of 14** diagnostic sinks with no bound against a doctrine in `perf/filesink.go` saying every one must have one, plus a ninth that re-implements the shared trim by hand | **build** — a jail-side diagnostic tier, and the two mechanical fixes (the dead conjunct, the unbounded sinks) that are gated on no ruling |
| 32 | Whether an implicitly forwarded port is disclosed, and whether the orphan reclaimer keeps killing | [`wire-bridge-port-collision.md`](../design/wire-bridge-port-collision.md) · in-review | 2 | [`OQ-PC2`](../design/wire-bridge-port-collision.md#oq-pc2) · [`OQ-PC3`](../design/wire-bridge-port-collision.md#oq-pc3) | **The defect underneath is FIXED and the doc is `BUILT`** (`3c20a5d8`): `ComposeProviders` deep-copies each provider value at its read, so `adaptEndpoints` no longer writes the adapter's own `127.0.0.1:8214` back through an alias into the user's config map, and `localProviderForwards` stops reading it out as a host-loopback forward the user asked for. **Both questions survive the fix untouched**, which is why this is a ruling row and not a done one: `briefingPortsFor` still feeds the briefing from the config section alone, so a port nobody wrote is bound in the jail and named in no briefing, banner or `launch.log`; and `reclaimOrphanedJailDaemons` still `SIGKILL`s on an argv match, from an inference about ownership, against a failure nobody has observed. ⚠ **[`OQ-PC2`](../design/wire-bridge-port-collision.md#oq-pc2) is cited by id from [row 31](#-needs-you)'s doc**, so it stays live in this doc rather than moving — which is also what defers the graduation into [`wire-bridge.md`](../reference/wire-bridge.md) that a `BUILT` doc would otherwise be due | **one ruling each, and they are independent** — disclosure is one launch line plus a briefing source change; the reclaimer is keep, narrow or revert |
| 33 | How a configured LSP server reaches Claude, and why only three languages are hardcoded | [`claude-lsp-plugins.md`](../design/claude-lsp-plugins.md) · in-review | 3 | [`OQ-LSP1`](../design/claude-lsp-plugins.md#OQ-LSP1) | **build** — the three plugins are hardcoded in two places ([`packs/claude/derive.lua`](../../packs/claude/derive.lua), [`internal/entrypoint/claude.go`](../../internal/entrypoint/claude.go)) and are a subset of the thirteen the marketplace publishes; each is a bare `lspServers` declaration yolo's own `lsp_servers` table already expresses. Implicated in the `enabledPlugins` over-drop defect already on this roadmap, and the remaining reason the `claude_plugins` hook exists |
| 34 | What a `kind` is, and how a contribution addresses an agent — a slot as a second axis, not a contribution shape | [`slots-and-contributions.md`](../design/slots-and-contributions.md) · in-review | 4 | [`OQ-D1`](../design/slots-and-contributions.md#OQ-D1) | **design + build** — `agent` vs `agents` is a singular/plural flag standing in for two opposite roles, and it already produced the alias-root bug. Splits `contributes` (kinds) from `exposes` (slots) and addresses a slot by the agent `bin` name. **Supersedes the `files` slot shape just landed** |
| 35 | What a pack manifest is written in, once the model stops repeating itself | [`manifest-language.md`](../design/manifest-language.md) · in-review | 4 | [`OQ-M1`](../design/manifest-language.md#OQ-M1) | **design** — `packs/claude/pack.json` gzips to **0.24**, more redundant than the repo's prose, because it is a flat tagged union that repeats `kind` ×17 and the pack's identity ×6. Separates the syntax choice from the model change (which is syntax-free and carries most of the win) |

**Rule together, or not at all.** [`E1`](BACKLOG.md#-e1--collapse-host_files-modes-43-copy-merges-into-readonly) · [`E2`](BACKLOG.md#-e2--readonly-as-a-real-ro-mount-instead-of-0o444) · [`OQ-B`](pack-host-management-plan.md#open-questions) are one asymmetry seen three times, and each doc says so.
[`OQ-BR3`](../design/bedrock-plumbing.md#OQ-BR3) and [`OQ-PS3`](../design/provider-switching.md#OQ-PS3) are the same decision in two files.
[`OQ-PS6`](../design/provisioner-sets.md#OQ-PS6) · [`OQ-PS7`](../design/provisioner-sets.md#OQ-PS7) · [`OQ-PS12`](../design/provisioner-sets.md#OQ-PS12) together release [§9](../design/provisioner-sets.md#9-what-i-would-build-in-order) step 4, and nothing else does.
[`OQ-WP8`](../design/workspace-path-mirroring.md#OQ-WP8) closes [`OQ-WP1`](../design/workspace-path-mirroring.md#OQ-WP1) and [`OQ-WP6`](../design/workspace-path-mirroring.md#OQ-WP6) with it.

**Small calls that do not deserve a row**, each blocking nothing by its own doc's account:
[`CFP-1`–`CFP-3`](../design/composed-file-permissions.md#open-questions) (graduated; three postures) ·
[`OQ-9`](../design/agent-auth-modes.md#OQ-9) (AWS's two-part credential already works) ·
[`OQ-LP5`](../design/loophole-packaging.md#oq-lp5) and [`OQ-LP7`](../design/loophole-packaging.md#oq-lp7) (cheap to rule, expensive to discover later) ·
[`SS-6`](../design/jail-state-separation-design.md#ss-6) (file an upstream mise issue; unverifiable from in here) ·
[`Q2`](../design/macos-user-build-step-threat-model.md#-q2--is---accept-flake-config-worth-the-substituter-poisoning-surface) (keep `--accept-flake-config`, or drop it) ·
[`OQ-B`](pack-host-management-plan.md#open-questions) (`0o444` at the host — explicitly taste).
🤷 **Genuinely subjective, wherever they sit:** [`OQ-PS4`](../design/provider-switching.md#OQ-PS4) ·
[`OQ-RM4`](../design/reference-mismatch-diagnostics.md#OQ-RM4) ·
[`OQ-WP7`](../design/workspace-path-mirroring.md#OQ-WP7) · [`OQ-WP12`](../design/workspace-path-mirroring.md#OQ-WP12) ·
[`OQ-BP-3`](../design/backend-parity.md#open-questions) · `OQ-B`.

## 📦 Ready

Ruled, unblocked, and implementable cold — no memory of any conversation required.

| | Build | Why it is ready | Where |
|---|---|---|---|
| 1 | Give the OpenAI subscription service a `macos-user` refresh consumer, and promote `openai-auth` to a public verb | **The Apple Container half and host-only import/logout are DONE 2026-09-18** (`36c47baa`, `4de78ac0`). The `withoutOpenAIAuthPack` exemption is deleted, so no pack is exempt from the inert report and the one service that backend starts is reported with `backendInertReason`'s version-stamped measurement instead of being the single pack the launch said nothing about. Withholding the endpoint variable was deliberately NOT done, with the reasons in `openaiauthbackend.go`: the measurement is per BACKEND, so it would patch one service's pointer against a fact covering every loopback-TLS service there, and the adapter reads that variable per request — withholding it keeps the process and loses the diagnosis. Import/logout are two HANDLERS chosen where the sockets are bound, never a `JailID` check (that field is self-asserted on the private socket); import refuses a symlink, an incomplete token set and a yolo broker view, and the refusal is asserted through the real loopback-TLS front. ⚠ Still hidden behind `yolo internal openai-auth` — promoting it is a registry row plus two help tables. ⚠ The `macos-user` half waits on 💬 [row 23](#-needs-you), and the four PARTIALs from the 2026-09-17 audit are untouched: no Codex version floor, no pi ask-once-more, no port for a third concurrent login, and `Broker.Logout` now has a caller but no public verb | [`openai-auth-broker.md`](../design/openai-auth-broker.md) · [`openai-auth-broker-plan.md`](../design/openai-auth-broker-plan.md) |
| 2 | `compat` facts are PROVIDER-DECLARED — **BUILT** | **DONE 2026-09-18** (`2697f8ac`). Six `options` on `packs/llamacpp`'s provider, each translated by `packs/pi/derive.lua` into pi's compat field — declare, don't detect, with no name check and no URL check anywhere in the path. Adding ollama or vLLM is a pure-declaration change and the recipe is in `packs/llamacpp/README.md`; **no flag values were invented for either**, because neither the research doc nor pi's bundle carries a measured set and fabricating one is the downgrade this ruling refuses. 8 mutations applied, 8 caught, including deleting the call site — the end-to-end composes the SHIPPED manifests through `ConfigurePackSurfaces` and reads `models.json` back, with zai as the selectivity half (shipped, declares none, must get no `compat` key). ⚠ **Two traps it had to disarm, either of which would have made this silently wrong.** The values were transcribed from pi **0.85.1**'s own bundle rather than from the research doc, whose example JSON still carries `supportsUsageInStreaming: false` — a value its own re-verification subsection records as having flipped to `true`; copying the example would reinstate a measured-wrong flag. And an option arrives as the STRING `"false"`, which is truthy in JavaScript, so passing it through unconverted would have told pi the capability is ENABLED. The derive converts and the test asserts the type. ⚠ **A pre-existing precedence wart was followed, not fixed**, and it is a candidate of its own: the neighbouring reads are provider-options-first and profile-second, so a profile's explicit override of an option the provider gives a DEFAULT for loses. That already affects `context_window` and `max_tokens`. The user-facing override path (`providers.llamacpp.options`, which composes per field) is correct | `packs/pi/derive.lua`, `packs/llamacpp/pack.json` |
| 3 | Start `jail_daemon` on `macos-user` — the decline shipped; the daemon waits on 💬 [row 23](#-needs-you) | **The truthful decline SHIPPED 2026-09-18** (`f6387968`): `loopholes.Set.JailDaemons` is the one composer and `JailDaemonPayload` the one writer of the wire shape — `internal/cli/run` had been building a second copy beside it — hoisted above the backend dispatch, with the container argv pinned byte-identical both as a golden and as the equality of the compose-inside and compose-outside spellings. The native arm declines each daemon BY NAME. **Measured on the default config, and it corrects this row's own prediction: a bare `"packs": ["claude"]` declares THREE, not two** — `oauth-terminator` as well — and none runs, while claude's host broker singleton DOES start there. No classifier was written: nothing is runnable on that arm, so its second branch would have been guesses. Steps 3–4 still need the two unfiled rulings (argv resolution with no image; confinement). The 📦 row 2 dependency is CLEARED (`eb02ad86`) — a spawn failure is now readable instead of presenting as an empty log and no process | [`jail-daemon-on-macos-user-plan.md`](../design/jail-daemon-on-macos-user-plan.md) · `internal/loopholes`, `internal/macosuser` |
| 4 | Bedrock from a host `aws sso login`: the jail-side adapter, the pack, and the live-host checks | **Steps 1–4 BUILT 2026-09-18** (`b94351fe`, `e76e43b2`, `700d7699`, `ae635641`, `9fc4879d`, `ec788fbb`). `internal/awscredadapter` is a pass-through by construction — the host handler's JSON *is* the container-credentials body, so it picks the HTTP status and nothing else — and `packs/aws-auth` ships the manifest, the four user-scope settings keys, the `bedrock`-gated pointer and the census rows. **A nested jail reached design done-condition 2**: a `curl` inside the jail got the four keys through the real authenticated front, with a fake `aws` on the launcher's PATH. ⚠ **And it is NOT REACHABLE as shipped — [Blocker 7](#rule-these-first), measured, not reasoned:** `hostServicesMountArgs` emits `YOLO_SERVICE_<NAME>_ENDPOINT` for two host-scoped loopholes **by name**, and `aws-auth` is the third, so the adapter answers every request with `ServiceUnreachable` while the launch reports a healthy jail — the reachability witness walks a variable that is never emitted. Blockers 1 and 2 are spent; **2 landed on [`OQ-BR4`](../design/bedrock-plumbing.md#OQ-BR4)** — `aws-auth` installs no CLI, so its `bedrock` gate takes `EnvFold`'s wide pass and `-p bedrock -- claude` sets the credential URI **jail-wide**, the first instance where both of that question's horns bite. **Blocker 6 is still open and now sits under a shipped feature**: the un-narrowed disclosure has two of its three call sites, and the third needs a declarative `loopholedecl` vocabulary rather than the tool-name switch. **Step 6 BUILT 2026-09-18** (`93068748`) and it is the one that needed no host: a jail carrying BOTH AWS credential arms is refused, and the rule lives in a package of its own (`internal/awschain`) keyed on the VARIABLE an SDK reads and the CAPABILITY a loophole declares — never on a name, which is what Blocker 6 forbids. So it fires for a user who writes the pointer by hand and for whatever ships the same channel next. `yolo check` predicts it through the same function (the `protocols.go` precedent, not `capabilities.go`'s copy), and the `~/.aws` grant conflict is a `ValidateConfig` error, which is why Blockers item 4 is RESOLVED. ⚠ **The mutation worth reading is the resolver seam**: every `internal/config` test injects a `fakeResolver`, so dropping `Serves` from `Resolver.Known()` would have left the real rule dead with the whole suite green. Steps 5, 7 and 8 remain. ⚠ Nothing has run against a live `aws sso login` | [`sso-backed-bedrock.md`](../design/sso-backed-bedrock.md) · [`sso-backed-bedrock-plan.md`](../design/sso-backed-bedrock-plan.md) |
| 5 | `yolo config`'s target resolution — **BUILT AND GRADUATED** | **ALL SEVEN STEPS SHIPPED 2026-09-18** (`3330367c`, `bbb6eccc`, `9c284f0c`, `8b4bdd05`, `6019c1e0`, `369c6f63`, `ad39ce0e`). **Step 6**: the preview composes `Surface.HostSource` — the copy the LAUNCH stages — and reports the layer *unavailable* where no staged copy is reachable, rather than substituting the destination, which after one boot is yolo's own output. Blocker 1 is closed by a **fifth `YOLO_HOST_LAYERS` disposition** on the same wire (`entrypoint.HostLayerWire.Rendered`): labelled bytes are a BASELINE, never a layer, and never refuse a launch. ⚠ It is keyed on the **mark** — the host provenance record — not on the posture, because an absent `host_management` resolves to `assert`, so a posture test would have stopped every default install's jail composing the user's own settings file; [the host-layer section](../reference/config-target-resolution.md#the-host-layer--a-staged-copy-a-baseline-or-nothing) carries the correction. **Step 7**: a host-side `reset` at a jail notch needs no `--force`, acts on `<ws>/.yolo/home/…`, and refuses while that workspace's jail runs — or while the runtime cannot be asked (Blocker 4 answered tri-state; `--force` reaches both refusals, since narrowing it would retract a shipped hatch and the unqueryable case needs it most). The `unknown codec ""` residue is fixed rather than widened: a `user` surface takes its codec, defaults and managed flag from the matching `host_files` entry, leaving only the one case `reset user` exists for — an entry the user has since removed. ⚠ **Three residues, one of them a backend gap in a shipped feature:** `macos-user` emits no label at all, because its own plan builder writes the report, so a managed home's host file still composes as a LAYER there; the fifth disposition is unmeasured end to end through a managed home's launch; Blocker 3 is open, so `apply --sealed` keeps the bare-cwd walk, and the `--at` token parse is still duplicated. **Graduated 2026-09-18** — the as-built account is [`docs/reference/config-target-resolution.md`](../reference/config-target-resolution.md) and the design path is a redirect stub | [`config-target-resolution.md`](../reference/config-target-resolution.md) |
| 6 | Assert the `macos-user` Seatbelt profile at RUNTIME, and take three denies from Agent Safehouse | **ALL THREE PIECES SHIPPED 2026-09-18** (`ba340c7f`, `474f68a0`, `7cb98e3f`). The argv hole is closed — `PlanInvariants` now pins `sandbox-exec -f <this session's profile>` on the AGENT's argv, consecutively, and deleting the words from `LaunchArgv` turns a named test red. The three denies landed at the END of the profile, which is the whole risk: `(allow process-info*)` includes pidinfo and has always been last, so both new denies are inert anywhere above it and the file would still read as if they were in force. Base stays `(allow default)` — the inversion and the per-agent series were declined per [§9](../research/agent-safehouse.md). Every deny carries a `#seatbelt-test-id:` that ships inside the installed profile, and `integration/macosuserseatbelt_test.go` runs 15 cases under a real `sandbox-exec`, each with a bare control, so a refusal is attributed to the policy rather than to a missing file; a registry pins every id to its proof or to a written reason none exists, enforced both directions on LINUX under `-short`. ⚠ The new gate is cheaper than the launch gate, which would have let a run satisfy the vacuity check while every launch test skipped — so the verdict is now computed PER GATE and names the starved one. ⚠ **Nothing in the runtime half has executed**: it is written blind from a Linux jail, and what the first macOS run settles is the CONTROLS. Two ids are registered unproven on purpose with reasons (`/Volumes/Macintosh HD` is not on every Mac; no shell-level ioctl on a non-terminal succeeds unsandboxed, so the ioctl deny has no control and the allow beside it is measured instead). Two rulings still sit beside this and block nothing: [`OQ-AS1`](../research/agent-safehouse.md#OQ-AS1) (how far toward `deny default`) and [`OQ-AS2`](../research/agent-safehouse.md#OQ-AS2) (the per-agent doc series). **Newly possible, and small:** a `paths:`-filtered PR job for the policy suite alone — it needs neither nix nor a Mac account, so `macos-user.yml`'s "unmeasured launch cost" argument for keeping the workflow nightly-only does not reach it | [`agent-safehouse.md`](../research/agent-safehouse.md) · `internal/macosuser` · `integration/macosuserseatbelt_test.go` |
| 7 | Protocol resolution — **BUILT AND GRADUATED** | **ALL SEVEN STEPS SHIPPED 2026-09-18** (`25d46b7d`, `b2e6d744`, `db2cdfb1`, `b7373476`, `b054811e`, `6aa1578f`, `52419585`), every guard mutation-checked. An agent pack declares the wires its program speaks; an `adapter` contribution declares `from`, `to` and an address and nothing about who runs it; the resolver has all four outcomes — direct, adapted, refuse naming the pack to add, refuse naming both protocols. `8214`/`8215`/`8216` left their consumers for the adapter's own manifest, and the shorthand is now a removal refusal naming `endpoints.<protocol>.base_url`. ⚠ **Two done-conditions could not be met as written and are restated in the doc**: `internal/wirebridged` IS the adapter (a `yolo-jaild` subcommand dispatched by that literal name), so `rg -n wire-bridge internal/` can never return nothing — what holds instead is that **no rule in `internal/` names a pack, an adapter or a protocol as a condition**. ⚠ **The refusal did NOT land where the plan said.** The capability gate reads the merged user config only and cannot see a pack-shipped provider's endpoints, so a copy there would have refused a different set of launches; it landed at `packload.AgentEnv`, the one runner both notches reduce through and the only place with a resolved selection. ⚠ **MEASURED at composition and delivery only** — no jail has been launched against it, and `macos-user`, whose port collisions are why step 7 exists at all, is untouched. ✅ **The one follow-up is CLOSED** (`507997e4`): `yolo check` predicts the pairing refusal, and not as a second copy — `packload.PairingRefusals` is the gate `AgentEnv` applies, so neutering `refuseUnspeakableProvider` reddens the check test and three packload tests together. It cannot see `-p`, and says so where it is enforced. ⚠ **Step 6 also shipped a CI-only break**: the shorthand's removal left a fixture in `internal/config`'s tests that passes IN A JAIL and fails on a host, because the refusal is an error on the host and a warning in a jail — fixed with a `hostScope` helper (`76a3d238`), and `env -u YOLO_VERSION go test -short ./...` is the local reproduction | **GRADUATED 2026-09-18** to [`docs/reference/protocol-resolution.md`](../reference/protocol-resolution.md), and the graduation audit found four things FALSE against the tree. **The resolver never computes outcome 2, and structurally cannot**: `ComposeProviders` runs `adaptEndpoints` LAST, so by the time `ResolveProtocol` asks, an adapted address is just an address — there is no adapted branch, and `ProtocolResolution.Direct` has no production reader at all (both call sites discard the struct with `_`). **No launch line discloses an adaptation**: `packloopholes.go` classifies `KindAdapter: disclosureSkip`, beside `provider` and `service`. **“Core may not name a protocol” is true of the RESOLVER, not of core** — `packdecl`'s closed `wire_api` enum and `wirebridged`'s `routeFor` both carry protocol literals, so the checkable rule is the narrower one. And **the third port was eliminated rather than moved**: `packs/kilo` shares the single `openai → anthropic` adaptation now, so `8216` exists nowhere. ⚠ A fifth finding became [row 11](#-ready). ⚠ **31 inbound citations name a section number the stub no longer has** — not dead anchors, but each reads as verified while pointing nowhere; the stub carries the mapping table | [`protocol-resolution.md`](../reference/protocol-resolution.md) · `internal/packdecl`, `internal/packload`, `internal/wirebridged` |
| 8 | Repoint the inbound citations of five GRADUATED design docs, then delete the stubs that hold nothing | **This is the single thing gating every remaining graduation**, and no doc-only workflow can do it: the citations live in `internal/`, `packs/`, `integration/`, `.github/`, `flake.nix` and `AGENTS.md`. **THE SWEEP IS DONE 2026-09-18**, in three waves and nineteen commits: **221 citations repointed**, and the ones left are left with reasons rather than missed. **`docs/design/config-target-resolution.md` is DELETED** (`2e9191bc`) — zero inbound, which is the condition its own status line named. Four more stubs are at zero but for allowlisted records; three (`loophole-packaging`, `composed-file-permissions`, `jail-state-separation-design`) are correctly NOT deletable, because each still holds unruled questions. ⚠ **Three findings the sweep produced are worth more than the count.** A spelling both earlier waves missed: seven citations write the `loophole-packaging` doc's own section shorthand with **no `.md`**, invisible to any `<basename>\.md` scan. A repoint that would have been ACTIVELY WRONG: `loophole-packaging`'s `R5` is a doc-drift risk, and `loophole-system.md` has a DIFFERENT `R5` — the citation would have resolved and meant something else. And the earlier waves introduced their own defect, since fixed (`97d7c1cc`): six citations landed on the right reference file carrying a section number it does not have, which is the worst shape a citation can take because it resolves and reads as verified. Anchor resolution is now mechanically verified — **714/714 `docs/reference/*.md#anchor` fragments resolve tree-wide**, negative-controlled with an injected bad fragment. **What is left:** `flake.nix:92`, deliberately (one stale `darwin-image-provenance` path; editing that file invalidates every cached image on every machine, so it waits for a real flake change), and two sites whose material did NOT graduate — the `loophole-packaging` stub's evil-doctor finding (the loophole commands read config with no schema pass), which no section of the reference carries. Two findings came out of it. A `composed-file-permissions` citation pointed at the DEFECT REGISTER, which did not graduate and exists nowhere in the tree — correctly, since all five defects are closed (`3020d38a` records that and names `5add9e7e` as the only place left to read the probes). And `report-tiers.md`'s stub says it survives only for two citations that are now repointed, so it is deletable once the records are settled. The original measurement, for the code half: redirecting as `loophole-packaging.md` → [`loophole-system.md`](../reference/loophole-system.md) (63, the bulk), `the-load-sentinel-is-not-a-liveness-oracle.md` → [`image-retention.md`](../reference/image-retention.md) (34), `jail-state-separation-design.md` (14), `darwin-image-provenance.md` → [`image-staging-vs-baking.md`](../reference/image-staging-vs-baking.md) (11, a FOLD rather than a move — the section anchors change too), `composed-file-permissions.md` (9), `cerebras-pack-and-copilot-delivery.md` (8) and `report-tiers.md` (8) to their same-named reference copies. ⚠ **Count all four spellings or the answer is wrong by half**: `docs/design/x.md`, `../design/x.md`, and — from a `docs/design/` sibling — the bare `](x.md)` and `](./x.md)`. Counting the first alone said 77 in 57 and reported two stubs at zero that were not. Exclude `.claude/worktrees/`, which holds agent copies of the whole tree and inflates every number by an order of magnitude. Heaviest holders: `internal/cli/run` (14 files), `internal/loopholes` (9), `internal/prune` and `internal/loopholedecl` (6 each). ⚠ **Partition by FILE, never by doc** — several files cite two of the five, and staging cannot fix an overlapping working tree. ⚠ **`§N` numbers do not survive a graduation**: a reference doc has named sections, so a section-number citation must be re-resolved by CONTENT, not rewritten mechanically. ⚠ **No stub is at zero**, including the two whose citations are entirely in other docs (`report-tiers.md`, `composed-file-permissions.md`) — those two are the cheapest and are a good first slice. ⚠ `loophole-packaging.md` is NOT deletable even at zero — it still holds two unruled questions, so it keeps its filename and loses only its body | `docs/design/*.md` stubs · `internal/`, `packs/`, `integration/`, `.github/`, `flake.nix`, `AGENTS.md` |
| 9 | `broker-ca-and-nested-hosts.md` — **all three pieces BUILT** | **DONE 2026-09-18** (`07af9439`, `d5bb1e5d`, `0de243cb`). The `[SKIP]` level closed [`OQ-3`](../design/broker-ca-and-nested-hosts.md#7-open-questions): nine `r.ok` sites across five section files reported *“I did not look”* as a pass, and `hostFact` covers the other direction the ruling named — the NVIDIA section had no in-jail guard at all and told an in-jail reader their GPU setup was broken in four `[FAIL]`s. The drift guard is a source census, because converting nine sites is a one-time fix and the next in-jail guard gets copied from whatever sits beside it. ⚠ **[`OQ-1`](../design/broker-ca-and-nested-hosts.md#7-open-questions)'s sequencing item named the wrong file, and the doc is corrected**: `internal/svcendpoint` has ALWAYS minted with `crypto/x509` — the design holds it up as the MODEL, so it cannot also be the thing needing a port. The subject was `EnsureCAAndLeaf` in `internal/oauthbroker`, and it is ported: the five-exec openssl script, `ca.srl`, `leaf.cnf` and `server.csr` are gone and actively swept, and **the CA private key never touches disk** — nothing ever read it back; it existed only because `openssl x509 -req -CAkey` needs a path, and on disk it is standing authority to impersonate any host. The LEAF key necessarily stays, because the in-jail terminator serves TLS with it from another namespace. P-256 replaces RSA-4096, whose keygen tail threatened `BrokerSpawnTimeout` — and a slow mint surfaces as “singleton never bound its socket”, the exact misreported failure this doc is about. Nine mutations, nine caught. ✅ **The measurement the doc recorded as owed is MADE**: a nested launch minted its own CA, mounted the trio, and `openssl verify -verify_hostname` returned OK against the real files — which also spends the stated reason for `brokerEndpointIsUnpublishable`'s nested arm (corrected in place; see [Rule these first](#rule-these-first) row 1) | [`broker-ca-and-nested-hosts.md`](../design/broker-ca-and-nested-hosts.md) · `internal/cli/check`, `internal/oauthbroker` |
| 10 | Stop the image-cache reaper deleting the archive a concurrent launch is copying FROM | **Defect, live today, and it violates the doc's own P4.** `PruneImageCache` ([`internal/prune/imagecache.go`](../../internal/prune/imagecache.go)) sweeps **every** `*.tmp` under `cache/images` unconditionally, on the reasoning that such a file is a leftover from a crashed materialize. Since C9 an in-flight delivery writes exactly that shape into exactly that directory — `archiveTempPath` uses `.oci-archive.tmp` and `.docker-archive.tmp`, and `filepath.Ext` on either is `.tmp`. **Neither lock closes it, deliberately:** the housekeeping lock is held only across the load path's inspect-and-record and its callers are *allowed to skip*, and the image-copy lock is a different file on purpose ([`internal/image/copylock.go`](../../internal/image/copylock.go) states why folding them would park every reaper behind a four-minute copy). So one launch's housekeeping slot can delete the archive another launch is reading. **macOS-only in practice** — `deliverViaArchive` is the arm the VM-backed backends take; a rootless Linux podman copies straight into containers-storage and writes no archive at all. ⚠ The sibling sweeper already knows the answer: [`internal/prune/sweep.go`](../../internal/prune/sweep.go) skips a directory touched within its cutoff *precisely so it never yanks a `.tmp` from under a concurrent writer*. `imagecache.go` has no such floor. Three fix shapes and the choice is real: an age floor, a suffix the reaper is taught to skip, or making the sweep take the copy lock | [`minimal-disk-footprint.md`](../design/minimal-disk-footprint.md) (recorded under its `P4`) · `internal/prune`, `internal/image` |
| 11 | Make the codex route's listen address follow the same composed entry every other route does | **Defect, live today, and step 7 of protocol resolution created it.** `internal/wirebridged`'s `routeFor` selects its Codex branch on `agent == "claude" && providerName == "openai-codex"` and returns **before reading the composed provider entry**, handing back the Go constant `CodexResponsesListenAddr = "127.0.0.1:8215"`. A user's `adapters` override for `openai-responses->anthropic` rewrites the composed entry — and therefore claude's `ANTHROPIC_BASE_URL` — so **the agent dials the new address while the bridge still binds the old one**, and the request is refused. The sibling `openai->anthropic` route has no such gap: it derives its bind from `endpointBaseURL(entry, "anthropic")`, which is exactly where `adaptEndpoints` writes the override. ⚠ **The early return's stated premise has expired.** Its comment says `openai-codex` is *“Pi/Codex's built-in subscription provider, not a composed provider fact”* — true when written, and false since step 4 gave that provider the public Responses endpoint so the Codex route could resolve through the general rule. Reading the composed entry is now possible, and symmetry with the other route says it should. The residual judgement is whether the listen address is the ADAPTER's declaration or the PROVIDER entry's, which for the other route are the same value by construction | [`protocol-resolution.md`](../reference/protocol-resolution.md) · `internal/wirebridged/boot.go`, `packs/wire-bridge/pack.json` |
| 12 | A bare `-p` no longer refuses a non-agent command — **DONE**; what is left is the gap the withdrawal named | **DONE 2026-09-19** (`fix(run): a bare -p no longer refuses a non-agent command`). `yolo -p kilo -- sleep 60` died as `no pack installs a CLI named "sleep"`: `checkProfileTargets` paired the BARE form with `filepath.Base(o.Args[0])` and checked that basename against the installed-CLI namespace — **a keying nothing downstream performs**. `effectiveUseProfiles` ([`assemble.go`](../../internal/cli/run/assemble.go)) has keyed a bare `-p` off every bin of every SELECTED pack since the 2026-09-03 ruling, command or no command, so the pre-flight stood in front of a table it did not describe and the only thing it could block was a legitimate launch — a shell, a script, a probe, in a jail with a profile active. The ruling behind the withdrawal: a profile selects providers for the agents IN the jail and reaches them through the `~/.yolo/bin/launch/<name>` shims, so what CLI is passed into the jail stops mattering entirely. ⚠ **The explicit `-p <cli>=<name>` branch is deliberately KEPT** — that spelling NAMES a CLI, so `-p claud=kilo` asserts something false and no downstream reader would ever say so; the tests fail if either that branch or its call site is deleted. ⚠ **What is left is stated at the function and belongs to a different check**: a bare `-p <name>` whose selected packs install no CLI at all selects nothing and tells nobody. That is a gap in the SELECTION rather than in the command, and closing it needs a check that can see an empty effective table — not one whose whole input is a CLI name the user typed. ⚠ Two design docs still SPECIFY the withdrawn check and carry a supersession note rather than a rewrite ([`profiles-as-pack-variants.md`](../design/profiles-as-pack-variants.md), [`reference-mismatch-diagnostics.md`](../design/reference-mismatch-diagnostics.md)) | `internal/cli/run/packs.go`, `internal/cli/run/runcmd.go` |
| 13 | The wire bridge crashed on every healthy idle — **DONE**; the idle it now survives still refuses the launch | **DONE 2026-09-19** (`fix(wire-bridge): a healthy idle crashed the daemon instead of idling`), measured on a host. `Main` passed `context.Background()`, whose `Done()` is **nil**; `idleUntilStopped` received on it, and a receive on a nil channel with no other goroutine is a runtime-fatal deadlock. So the state this package's own doc calls *"bind nothing, publish nothing, sleep forever, one stderr line saying why"* printed the line and then died as `fatal error: all goroutines are asleep - deadlock!`, and the supervisor — `restart: on-failure` — was left restarting a daemon that could never stay up. **It is reached whenever a selected provider names a credential variable that is unset, or a Codex route has no credential endpoint**: both are DESIGNED idles, not faults. The fix is `signal.NotifyContext(… SIGINT, SIGTERM)`, which is also the path from `yolo-jaild supervise`'s stop signal to a clean exit that an idling bridge never had. ⚠ **The defect could not be pinned by reproducing it** — a runtime deadlock takes the test binary with it — so the tests pin the property instead: the context can be done, an idle returns when cancelled, and an **AST check that `Main` calls `daemonContext`**. Without that third one, reverting `Main` to `context.Background()` leaves the other two green, which is exactly the callee-pinned/call-site-unpinned shape AGENTS.md names. ⚠ **The idle it now survives still refuses the launch**: `serve` signals `failed <name> <reason>` before idling, and that is fatal at boot with no hatch — 💬 [row 30](#-needs-you) | `internal/wirebridged/boot.go`, `internal/wirebridged/idle_test.go` |
| 14 | Correct the four documents that understate what a `scope: "host"` declaration starts | **Ruled by the tree rather than by a decision — every claim below is checkable, and each one understates what runs on the machine.** The host-scoped set is **three** (`claude-oauth-broker`, `openai-auth-broker`, `aws-auth`); `rg -n '"scope": "host"' packs/*/loopholes/*/manifest.jsonc` is the whole census, and nothing else enumerates it, which is why the last two additions left four documents wrong at once. [`loopholes.md`](../guides/loopholes.md)'s manifest-schema census shows `host_daemon` with `cmd`, `env`, `publishes` and `request_end` and **omits `scope` entirely** — the author-facing schema does not document the key that creates a host singleton — while its comment says yolo *"spawns this ON THE HOST at jail startup"*, which is the per-jail lifecycle, not this one. [`agent-credentials.md`](../reference/agent-credentials.md)'s current-values table lists **two** `scope: "host"` daemons, and the string `aws-auth` appears nowhere in that file. [`USER_GUIDE.md`](../guides/USER_GUIDE.md)'s host-service Lifecycle list — *"When the container exits, yolo sends `SIGTERM` to each service, waits 5 seconds, then `SIGKILL`"* — has no host-scope carve-out, so it states the opposite of the ruling: a jail ending touches a singleton not at all. And [`AGENTS.md`](../../AGENTS.md) still spells `yolo internal daemon <claude-oauth-broker\|host-processes\|journal>`, where [`internal.go`](../../internal/cli/internal.go)'s own usage line names **six** (`aws-auth\|claude-oauth-broker\|host-processes\|journal\|openai-auth-broker\|serial`). ⚠ Four edits, none of them a ruling — but what would stop it recurring is [`OQ-HD7`](../design/host-daemon-ownership.md#OQ-HD7), enumerating the host-scoped set at launch the way host grants are already disclosed, and that IS a ruling and is not this row | [`host-daemon-ownership.md` §8](../design/host-daemon-ownership.md#8-one-became-three-and-nothing-noticed) |

## 🔒 Waiting

> [!IMPORTANT]
> **"A Mac" stopped being a blocker on 2026-09-14.** A self-hosted arm64 runner is registered and
> dispatches itself every five minutes on a new commit
> ([the runbook](runbooks/mac-actions-runner.md)), and `integration/applecontainer_test.go` runs
> five tests green on it. Four rows below still say "a Mac" — **they are now waiting on someone
> writing the test, not on hardware.** That is a much cheaper blocker, and two beliefs this repo
> held for months inverted the first time that hardware ran (apple/container#889 and #1089), so the
> expected value is high rather than confirmatory.

| | Blocked on | What clears it |
|---|---|---|
| [`OQ-WP5`](../design/workspace-path-mirroring.md#OQ-WP5) — deep bind destinations on Apple Container | a Mac; unmeasured since 2026-09-04 | one `container run` against a nested destination |
| `lsp_servers` on `macos-user` | a Mac; wired 2026-09-13, never run | `TestMacosUserDeclaredToolsArrive/lsp_servers` green |
| [`Q3`](../design/macos-user-build-step-threat-model.md#-q3--do-we-want-the-macos-nix-build-sandbox-on-for-yolo-triggered-builds) — the darwin nix build sandbox | a Mac | which packages assume an unsandboxed darwin build |
| Backend-parity's fourteen shipped macOS fixes | a Mac — **one exists now**: a self-hosted arm64 runner landed 2026-09-14 | four ran that day, all green (macOS 26.5, container 1.1.0): the jail starts, #39's machine-wide tier arrives, #44's pack tree is readable, and `:ro` is HONORED — which retires the premise under `roBindsUnsupported`. The rest are still unrun |
| [`OQ-WP9`](../design/workspace-path-mirroring.md#OQ-WP9) — a content-based venv oracle | a measurement | whether reading the recorded interpreter is cheap enough per launch |
| The `llamacpp` mode, end to end | a live `llama-server` — nothing in it has ever run against one | the per-agent smoke tests in [`packs/llamacpp/README.md`](../../packs/llamacpp/README.md). Two are load-bearing rather than confirmatory: **copilot** runs the anthropic endpoint against llama-server's `/v1/messages`, a pairing nobody has seen complete, and its result decides whether the pack should ship that endpoint at all; **codex** is a NEGATIVE test — `~/.codex/config.toml` must carry no `[model_providers.llamacpp]` and no selection, because a half-landed selection there is the bug |
| The image-copy lock, under contention | a ROOTLESS host | several jails launched at once against a cold store: one `image.layer_copy` span and N−1 `image.copy_lock` waits, reported with `podman info --format '{{.Host.Security.Rootless}}'`. A nested jail is rootful and structurally blind to it |

## 🧊 Icebox

| | The uncertainty | What would thaw it |
|---|---|---|
| [`boundary-broker.md`](../design/boundary-broker.md) — a generic approval broker | its own [§5](../design/boundary-broker.md#5-three-tiers-not-two--and-git-wants-the-middle-one) half-undercuts the premise: the motivating GitHub case probably wants a proxy, not a human | a concrete instance the current model blocks. One now exists and deliberately declined to build it: [`OQ-SSO6`](../design/sso-backed-bedrock.md#13-decision-ledger) ruled a lapsed AWS SSO session a message the jail reports, never a request it files, because half an approval mechanism inside a credential pack is the second front door this doc exists to prevent. The first consumer is identified and waiting rather than hypothetical |
| [`cache-relocation.md`](cache-relocation.md) — `yolo cache relocate` | all three questions are HELD by choice: what is undecided is whether we want the feature | ruling [`OQ-CR1`](cache-relocation.md#-oq-cr1--is-cache_relocations-the-right-level-held), which carries CR2 with it |

## What this file does not cover

**Candidate work nobody committed to.** [`further-roadmap-ideas.md`](further-roadmap-ideas.md#1-the-five-i-would-build)
carries five *build it* verdicts — a decision-link checker, a NOTHING-BUILT detector, an
orphaned-program report, one jail-blindness concept in `check`, and the reachability class in
CI. That file is a shelf by its own rule, and none of the five is a task until someone wants it.

**Live questions that block nothing** are listed under *Small calls* above rather than given
rows; they are real and cheap, and none of them is holding anything up.

**Questions with no id, invisible to the count command.** Whether the three `packages:`-declaring
tests should SKIP rather than fail on a builder-less runner
([`handoff-mac-unmeasured-claims.md` §4](handoff-mac-unmeasured-claims.md#4-the-nightly--five-links-all-now-named)).
Whether Go comments should cite a reference doc by named section, and what becomes of the 211
dangling citations that census found ([`doc-triage.md`](doc-triage.md#the-2026-09-12-graduation-assessment--the-five-docs-the-sprint-built)).
And the two that gate 💬 [row 23](#-needs-you): how a declared `jail_daemon.cmd` resolves on a
backend with no image, and whether that daemon runs under the Seatbelt profile — stated in
[`jail-daemon-on-macos-user-plan.md`](../design/jail-daemon-on-macos-user-plan.md)'s §Blockers,
which is the only place they exist. All four want an id before they want a row.

**Doc closures that are bookkeeping, not decisions.** Four docs graduate next, in a ruled order
([`doc-triage.md`](doc-triage.md#recommendation)); `report-tiers` went 2026-09-13 and left two
links to repoint. They are work, not questions.

*A question is promoted to a row the day it starts blocking something, and leaves the day it stops.*
