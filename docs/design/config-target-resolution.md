---
title: "Which home does `yolo config` mean? — graduated to the reference tree"
date: 2026-09-16
status: accepted
tags: [design, config, cli, notch, workspace, capture, disclosure, graduated]
summary: "A stub. The config-target design is built and its as-built account is docs/reference/config-target-resolution.md — the one resolved target, the marker, the disclosure, the fifth host-layer disposition, the host-side jail-notch reset, the three live gaps and every OQ-CR ruling live there. This filename survives only while inbound references are repointed."
vantage:
  status-chip: true
---

# Which home does `yolo config` mean? — graduated to the reference tree

**Status:** GRADUATED, 2026-09-18 — the settled body of this doc moved to
[`../reference/config-target-resolution.md`](../reference/config-target-resolution.md).
Nothing is owed here.

> [!IMPORTANT]
> **The design is BUILT, and this file is no longer where it is described.**
> [`../reference/config-target-resolution.md`](../reference/config-target-resolution.md) is the
> as-built account: the one resolved **config target** and why the defect was the *pair* rather
> than the inference; P1–P7, including **P6**, the rule that yolo never reads back a file it
> writes, and **P7**, that adopting yolo requires no migration; the artifact marker and the
> walk's boundary stop; the unconditional disclosure and why it is on **stderr**; the four
> store states and three surface reaches; the fifth `YOLO_HOST_LAYERS` disposition, **keyed on
> the provenance mark rather than on the posture**; the host-side jail-notch `reset` with its
> tri-state liveness refusal; the three gaps that are still live; and the
> [`## Why it's this way`](../reference/config-target-resolution.md#why-its-this-way) appendix
> where every `OQ-CR` id resolves, with its original anchor.
>
> **This stub carries no live question** — all eight were ruled on 2026-09-17 and all seven
> build steps shipped on 2026-09-18. It is not the shape
> [`composed-file-permissions.md`](composed-file-permissions.md) and
> [`loophole-packaging.md`](loophole-packaging.md) use, which hold questions a reference may
> not carry.

**Why it still exists.** Inbound references. The `OQ-CR` ids are cited from Go comments across
`internal/cli`, `internal/cli/run` and `internal/entrypoint`, and this path is named in the
same comments and in several tests; a separate citation sweep owns repointing them at the
reference, whose anchors are the same ids. Point them at
[`../reference/config-target-resolution.md`](../reference/config-target-resolution.md) — the
`#oq-cr1`…`#oq-cr8` fragments resolve there unchanged — and **delete this file**, which is what
the graduation would otherwise have done here.

The build hand-off retired with it:
[`config-target-resolution-plan.md`](config-target-resolution-plan.md).

## 10. Decision Ledger

**Moved.** Every `OQ-CR` ruling now lives in
[`../reference/config-target-resolution.md`](../reference/config-target-resolution.md#why-its-this-way)'s
*Why it's this way* appendix, with its original id and anchor. This heading survives only so
that citations of it keep resolving until they are repointed; it goes with the rest of this
file.
